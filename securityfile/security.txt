include files using 'define()' method.
password hashing.
input filed autocomplete disable
use prepared statements preventing from sql injection.
use htmlspecialchars function to prevent from cross side scripting.
generate token to prevent from cross site request forgery.
hide errors (error_reporting(0)).
hide directory indexes (use .htaccess file "Options -Indexes").
id's shouldn't be write directly.
session hijacking ---> (session_regenerate_id(true))
https://www.cloudways.com/blog/php-security/ 
after completion of project whole code will be compress or encrypt